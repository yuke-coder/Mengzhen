# 2026-08-04 工作日志

## 移动端任务行视觉重构（task-list.tsx）

### 改动内容
1. **StatusBadge 去边框化**：移除 border + bg 背景容器，改为「状态色圆点 + 图标 + 文字」轻量行内样式，tabular-nums 保证倒计时数字等宽
2. **RepeatBadge 去边框化**：移除 border + bg 背景容器，改为纯文字色标签，图标缩小至 2.5
3. **移动端 TaskItem 布局重构**（isMobile 分支）：
   - 外层 p-3 改为 px-4 py-3.5 更合理水平留白
   - 第一行：任务名（15px）+ 重复标签 + 状态徽章，取消态加 line-through
   - 第二行：时间|时长|音量|渐入渐出，用 | 分隔符，tabular-nums 数字等宽
   - 第三行：音频信息行精简，图标缩小，+N 改为 rounded-full 药丸
   - 第四行：操作按钮从文字按钮改为 36px 圆形图标按钮，active:scale-90 触摸反馈
4. TypeScript 编译通过（tsc --noEmit 无错误）

### 第二轮重构（用户反馈"敷衍"后，引入 Impeccable skill）
- 引入 Impeccable 设计原则，重新审视视觉层级
- 移动端任务行全面重写：
  - 外层加 `rounded-2xl bg-card/40` 卡片感（无边框）
  - 任务名升级为 `h4 + text-[16px] + font-semibold + tracking-tight`，视觉权重提升
  - 元数据行改用 `·` 分隔 + `flex-wrap`，三级文字色阶（60% → 45% → 35%）
  - 音频行直接内联 size/duration 信息，不再调 audioInfoDisplay（减少层级）
  - 操作按钮分层：主操作「立即执行/恢复执行」= 文字+图标+emerald 实色块；次操作「取消执行」= 文字+图标+amber 浅色块；三级「编辑」= 图标+muted；破坏性「删除」= 图标+red 浅色块
  - 按钮高度统一 h-10，圆角 rounded-xl，active:scale 触摸反馈
- 用户明确要求：立即执行、取消执行、删除（带确认弹窗）三个操作必须存在
- 删除确认：onDelete → handleDelete → setDeleteConfirmId → FeedbackModal 弹窗确认（已有逻辑）
- TypeScript 编译通过

### 第三轮重构：信息架构重组（用户要求清晰展示任务形式和操作行为）
- 引入 Timeline 可视化：启播时间 → 时长 → 关闭时间，用时间线连接器展示
- StatusBadge 拆分为「状态标签 + 独立大号倒计时」两行布局
  - 状态文案语义化：等待渐入/等待开始/音量渐入中/播放中/音量渐出中/已完成·等待下轮/已取消
  - 倒计时 18px font-bold tabular-nums，状态色着色，视觉焦点
- Detail tags 行：volume/fadeIn/fadeOut/audio 各自独立 tag，带语义色（cyan=渐入、amber=渐出）
- TypeScript 编译通过（.next/types/validator.ts 的 Route 报错是 Next.js 生成的，与本次改动无关）

### 设计原则
- 遵循用户「全局去边框」偏好
- Impeccable: 视觉层级通过色阶对比而非边框/阴影实现
- 触摸友好：h-10(40px) 按钮 + active:scale 反馈
- 操作层级清晰：primary(执行) > secondary(取消) > tertiary(编辑) > destructive(删除)

---

## Web 端反馈页完整迁移深海圈源码 (src/app/feedback/page.tsx)

### 源码来源
- `C:\Users\kmzho\Downloads\深海圈官网建议反馈_网页资源.zip` → `scys.com/index.html`
- Vue SPA 预渲染 DOM，基于 Arco Design + Vite，品牌主色 `#36A590`

### 迁移内容（1:1 还原源码 #app DOM 结构）
- 顶部导航 vc-top-nav：a.logo 内嵌 base64 官方 logo（5KB+）+ v-spacer 占位 + me.name 用户名
- vc-container-pc 内补齐：
  - event-img 事件海报区（源码 display:none，含 poster/img.poster_bg/pc-event-btn-box/btn-2/立即报名/圆形箭头 SVG）
  - s-form 表单
    - field_type：radio 选项（Bug 缺陷 / 产品建议），保留源码 option.radio/circle/commodity-value 嵌套结构
    - field_content：textarea + images 容器（img-box.placeholder + text_paste 提示文案 + 隐藏 file input）
    - field_contact：textarea.border
  - 隐藏的微信订阅容器（display:none）：通过 dangerouslySetInnerHTML 注入 `<wx-open-subscribe id="subscribeBtn">` + `<script type="text/wxtag-template">` + 内联样式 button#subscribe-btn
  - 实际提交按钮：submit-wrapper.v-column-start > div.submit
  - form-success vc-pc 成功页容器（源码 display:none，React 通过 success 状态控制显示，内部填充现有青绿色 CheckCircle 成功 JSX）
  - form-success vc-pc event-poster 副容器（保持源码 display:none）
  - nav > vc-records-btn 右侧悬浮已填记录按钮（完整 SVG 5 个 path）

### 保留的源码远程资源
- 21 个源码 CSS URL（index.BNAygxs7.css 1.1MB Arco 核心 + 20 个业务 CSS）通过 useEffect 注入 document.head，组件卸载时清理
- 内嵌 .v-dropdown-container 样式块（源码 head 内嵌）
- 源码防闪烁主题脚本（theme-preference localStorage → data-theme 属性）
- 保留现有 `/css/feedback-scys.css` link（已是源码 4 个核心 CSS 的合并 + dark mode override + vc-form 透明背景 override）

### 保留的 React 逻辑
- 状态：type/content/contact/images/submitting/success
- 事件：handleContentChange/handleContactChange/handlePaste/handleFileChange/removeImage/handleSubmit
- autoResize textarea 自适应高度
- 登录态校验（loading/!user 早返回）
- 提交后调用 `/api/feedback` POST 接口

### TypeScript 适配
- 源码 inline style `resize: none !important` / `overflow: hidden !important` 不被 React CSSProperties 接受，用 `as unknown as React.CSSProperties` 断言保留源码原值
- `<wx-open-subscribe>` 微信自定义元素无法用 JSX 直接渲染，用 `dangerouslySetInnerHTML` 注入完整 HTML 字符串

### 验证
- `npx tsc --noEmit` feedback/page.tsx 编译通过
- 仅剩 `.next/types/validator.ts` 的 Next.js 自动生成错误（已知，与本次改动无关）