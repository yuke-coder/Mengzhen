<?xml version="1.0" encoding="utf-8"?>
<layer-list
  xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <clip>
            <bitmap android:src="@drawable/player_progress_right" />
        </clip>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <bitmap android:src="@drawable/player_progress_load" />
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <bitmap android:src="@drawable/player_progress_left" />
        </clip>
    </item>
</layer-list>
